import styled from "styled-components";
import {Button} from "@mui/material";

export const DeleteButton = styled(Button)`
  font-family: 'Roboto', sans-serif;
  padding: 8px 24px;
  font-size: 12px;
  font-weight: 500;
  line-height: 1.25rem;
`

export const StyledTitleBasket = styled.h1`
  font-weight: normal;
  font-size: 24px;
  font-style: italic;
`